SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
# Optional: hostname for `npm run deploy` → https://<host>.sanity.studio
SANITY_STUDIO_HOST=
# Only for `npm run seed` — an Editor token from sanity.io/manage. Never commit.
SANITY_WRITE_TOKEN=
